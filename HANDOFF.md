# บริบทตั้งต้น — มัธยม (mattayom)

อ่านไฟล์นี้ครั้งเดียวตอนเซสชันแรก กติกาประจำวันอยู่ใน `CLAUDE.md` แล้ว

## คนที่คุณกำลังทำงานด้วย

Tap — นักศึกษาปริญญาเอกที่ Sheffield ทำ agent-based modelling ของโครงข่ายรถไฟด้วย FLAMEGPU2
แข็ง Python กำลังสร้างความคล่อง C++/CUDA **ไม่ได้มาสายเขียนโค้ดแอป** TypeScript เป็นของใหม่

วิธีทำงานที่เขาขอไว้:
- ตอบภาษาไทย
- อธิบายโค้ดเป็นขั้นตอน ใช้เกมนี้เป็นเครื่องมือเรียนรู้ไปด้วย
- พูดตรง เห็นตรรกะพัง เห็นโค้ดผิด เห็นความเสี่ยง บอกเลย ไม่ต้องประคอง
- อ้างอิงต้องจริง ไม่รู้ให้บอกว่าไม่รู้

นี่เป็นโปรเจกต์งานอดิเรก ทำแบบ vibe code ค่อยๆ เกลาไปเรื่อยๆ ให้ใหญ่ขึ้น
ไม่มีเดดไลน์ ไม่ต้องรีบ แต่ต้องไม่พังสะสม

## เครื่องและวิธีเล่น

พัฒนาบน MacBook Pro M3 Pro เล่นบน Mac เป็นหลัก และเปิดบน iPad ผ่าน Wi-Fi วงเดียวกัน
`server.host: true` ใน vite.config.ts คือบรรทัดที่ทำให้ iPad เปิดได้ **อย่าลบ**
สองโปรเจกต์ใช้คนละพอร์ตเพื่อให้รันพร้อมกันได้

## การตัดสินใจที่ล็อกแล้ว (ไม่ต้องรื้อใหม่)

**TypeScript + Vite + Canvas** — เลือกเพราะมันแก้โจทย์ "พัฒนาบน Mac เล่นบน iPad" ได้ทันที
โดยไม่ต้องมี Xcode ไม่ต้องจ่าย Apple Developer ไม่ต้อง build ใหม่ทุกครั้ง
Godot/Unity ถูกตัดออกด้วยเหตุผลนี้ ไม่ใช่เพราะมันแย่ ถ้าวันหนึ่งเกมต้องการ 3D
หรือ physics engine จริงจัง ค่อยคุยกันใหม่ — ตอนนั้นเหตุผลจะเปลี่ยนไปแล้ว

**หนึ่งโปรเจกต์ = หนึ่ง git repo** ไม่รวมเป็น monorepo

## เกมนี้คืออะไร

เกมชีวิตในโรงเรียน ผสม Persona 5 + Bully + dating sim แต่**ฉากเป็นโรงเรียนไทย**
ไม่ใช่โรงเรียนญี่ปุ่น นี่คือจุดต่างที่ตั้งใจ — แนวนี้ทำญี่ปุ่นกันจนล้น
แต่แทบไม่มีใครทำชมรม เข้าแถวหน้าเสาธง ไหว้ครู รุ่นพี่รุ่นน้อง กวดวิชา สอบเข้ามหาลัย
ซึ่งเป็นวัตถุดิบที่เล่นเป็นกลไกเกมได้หมด และ Tap มีประสบการณ์ตรง

โครงที่วางไว้: ปฏิทิน + ช่วงเวลาต่อวันจาก Persona, ก๊กและความซ่าในโรงเรียนจาก Bully,
ค่าความสัมพันธ์และเส้นเรื่องแยกจาก dating sim

## การตัดสินใจทางสถาปัตยกรรมที่สำคัญที่สุด

**บททั้งหมดเขียนใน ink (ของ Inkle) ไม่ใช่ใน TypeScript**

เหตุผล: งานที่ฆ่าโปรเจกต์แนวนี้คือ**ปริมาณเนื้อหา** ไม่ใช่โค้ด (Persona 5 มีบทหลักระดับแสนคำ)
ถ้าเขียนบทลงในโค้ด พอถึงตัวละครที่ 5 จะแตะอะไรไม่ได้เลย

ทดสอบสะพานสองทางแล้วว่าทำงานจริง:
- ฉีดค่าสถานะจาก TS เข้า ink → ตัวเลือกที่ต้องใช้ `{mind >= 6}` ปลดล็อกถูกต้อง
- `~ gainAffinity("ploy", 3)` ใน ink เรียกกลับมาที่ TS ได้
- ข้อความไทยผ่านทั้งคอมไพเลอร์และ runtime ไม่มีปัญหา

ตอนนี้คอมไพล์ ink ตอนรัน (ผ่าน `inkjs/full`) เพื่อให้แก้บทแล้วเห็นผลทันทีผ่าน HMR
แลกกับ bundle 261KB วันไหนบทเยอะจนคอมไพล์ช้า ค่อยเปลี่ยนไป precompile เป็น .json

## สถานะปัจจุบัน

มีแล้ว:
- ปฏิทิน 4 ช่วงเวลา/วัน เทอมละ 120 วัน คาบเรียนล็อกอัตโนมัติวันธรรมดา เสาร์อาทิตย์ว่าง
- ค่าสถานะ 5 ตัว (ใจ ปัญญา เสน่ห์ น้ำใจ ความซ่า) มี 8 ระดับชื่อไทย
- ความสัมพันธ์ 10 ระดับ
- สถานที่ 7 แห่ง แต่ละแห่งมีกิจกรรมเพิ่มค่าสถานะ และมีตัวละครโผล่ตามช่วงเวลา
- ระบบพลังงาน เซฟอัตโนมัติลง localStorage
- ตัวเลือกที่ปลดล็อกด้วยค่าสถานะ
- ตัวละคร 3 คน (พลอย กนิน มินตรา) แต่**มีบทเฉพาะของพลอย** และเขียนถึงแค่ระดับ 1

typecheck ผ่าน lint:ink ผ่าน build ผ่าน

## ทำอะไรต่อได้ เรียงตามที่ผมคิดว่าคุ้มที่สุดก่อน

1. **เขียนบทกนินกับมินตรา ระดับ 0-1** — ยังไม่ใช่เพื่อเนื้อหา แต่เพื่อ**พิสูจน์ว่า pipeline
   ขยายได้จริงโดยไม่แตะโค้ด** ถ้าตรงนี้ติด ต้องแก้สถาปัตยกรรมก่อนเขียนบทเยอะ

2. **ฉากจบเทอม + ระบบสอบ** — ตอนนี้เล่นถึงวันที่ 120 แล้วเจอข้อความ TODO
   ปิดลูปให้ครบก่อน แล้วค่อยขยายเนื้อหาตรงกลาง

3. **ระบบชมรม** — เป็นทั้งตัวสร้างตารางเวลาประจำและเป็นที่ที่ตัวละครไปรวมกัน
   ส่วนของ Bully ที่ยังขาดมากที่สุด

4. **ระบบเงิน + ร้านค้า**

5. **ภาพตัวละคร** — ทำทีหลังได้ ไม่กระทบสถาปัตยกรรม แต่กินเวลามากที่สุด

## ข้อจำกัดที่รู้อยู่แล้ว

- ไม่มีภาพตัวละคร ไม่มีเสียง
- ไม่มีระบบสอบ ไม่มีฉากจบเทอม
- ยังไม่มีเหตุการณ์ตามปฏิทิน (วันไหว้ครู กีฬาสี สอบกลางภาค)
- เซฟมีช่องเดียว ไม่มี multiple save slot

## ข้อความที่ Tap เอาไว้พิมพ์ในเซสชันแรก

```
อ่าน HANDOFF.md กับ CLAUDE.md แล้วสรุปให้ฟังสั้นๆ ว่าเข้าใจโปรเจกต์นี้ว่ายังไง
จากนั้นรอผมบอกว่าจะทำอะไรต่อ
```
