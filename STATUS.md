# สถานะล่าสุด — 16 ก.ย. 2026

บันทึกสำหรับกลับมาทำต่อ · กติกาประจำวันของแต่ละเกมอยู่ใน `CLAUDE.md` ของ repo นั้น
บริบทตั้งต้นอยู่ใน `HANDOFF.md` ไฟล์นี้บอกแค่ว่า "ตอนนี้ถึงไหนแล้ว"

## วิธีลองเล่น

ทั้งสองเกมเป็นเว็บ ไม่ต้องติดตั้งอะไรบนเครื่องที่จะเล่น

```bash
# บน Mac — เปิดสองหน้าต่าง terminal หรือใส่ & ท้ายคำสั่ง
cd ~/tap-games/genesis  && npm run dev     # ปฐมเทพ  → http://localhost:5173
cd ~/tap-games/mattayom && npm run dev     # มัธยม   → http://localhost:5174
```

**บน iPad** — ต้องอยู่ Wi-Fi วงเดียวกับ Mac แล้วเปิด Network URL ที่ vite พิมพ์ออกมา
(เลข IP เปลี่ยนได้เมื่อย้ายเครือข่าย ให้ดูจากบรรทัด `Network:` ทุกครั้ง)

```
http://<ไอพีของ Mac>:5173/     ปฐมเทพ
http://<ไอพีของ Mac>:5174/     มัธยม
```

หา IP ได้ด้วย `ipconfig getifaddr en0` (Wi-Fi) ถ้าไม่เห็นบรรทัด Network
ทั้งสองเกมตั้ง `server: { host: true }` ไว้แล้ว **อย่าลบบรรทัดนั้น**

เพิ่มลงหน้าจอโฮมของ iPad ได้ (Safari → แชร์ → เพิ่มไปยังหน้าจอโฮม)
จะเปิดแบบเต็มจอไม่มีแถบเบราว์เซอร์ เพราะมี manifest กับ meta ของ PWA ครบแล้ว

**Mac เล่นด้วยเมาส์ได้ทุกอย่าง** ปฐมเทพลากเพื่อหมุนกล้อง ล้อเมาส์ซูม
**iPad คือเป้าหมายหลักของปฐมเทพ** — การลูบสัตว์ การตี และการหยิบของขว้าง
ออกแบบมาสำหรับนิ้วก่อน (ลากบนตัวสัตว์ = ลูบ · ปัดลงเร็ว = ตี · แตะค้างบนของ = หยิบ)

ปิดเซิร์ฟเวอร์ด้วย Ctrl+C หรือ `pkill -f "vite"`

## เทสต์ — รันก่อนเชื่อว่าอะไรทำงาน

```bash
cd ~/tap-games/genesis
npm run check                # tsc
npm run unit                 # 70 เทสต์
npm run sim 20 8             # 8 โลก · ดูบรรทัด "เตือน:" ถ้ามี = มีปัญหา
npm run visual               # 17 เทสต์ (ภาพ 11 + โมเดล/ท่าทาง/ความเร็ว)

cd ~/tap-games/mattayom
npm run check
npm run unit                 # 212 เทสต์
npm run lint:ink             # 48 ไฟล์บท
npm run story                # ทางตัน + ธง 185/185 + เสียง 21/21 + สะพาน 43/43 + สีหน้า 10/10
npm run playthrough          # เล่นจนจบสองภาค × 6 ภูมิหลัง ด้วยบทจริง
npm run balance              # 5 กลยุทธ์ × 5 seed + 6 ภูมิหลัง · ดูบรรทัดที่ขึ้นต้นด้วย ←
npm run audit                # ความสมเหตุสมผลข้ามข้อมูล
npm run visual               # 32 เทสต์ (ภาพ 14 + มินิเกม 7 เปิด + 7 เล่นจนจบ + ย้อนอ่าน/ออโต้ + แชท)
```

**รายการฟังก์ชันทั้งหมดของเกมอยู่ใน `FEATURES.md`**

**สถานะตอนนี้: ผ่านหมดทั้งสองเกม ไม่มีคำเตือนเหลือ**

## ระบบที่มีอยู่ตอนนี้

### ปฐมเทพ
ศรัทธา → ปาฏิหาริย์ 7 คาถา (คุยกันได้) → สัตว์ที่สอนได้ → หมู่บ้านที่ร้องขอ → แกนธรรม/อธรรม
· ชาวบ้านรายคน หยิบขึ้นมาได้ วางคืนดีๆ กลายเป็นนักบวช
· มือหยิบของขว้าง (หิน ต้นไม้ อาหาร คน) มีวิถีโค้งจริง
· สัตว์: ลังเลก่อนลงมือ · ความกลัว/ความอยากรู้ · **แอบทำตอนพระเจ้าไม่มอง** ·
  เรียนจากการดูคาถาและดูชาวบ้าน · ท่าหลายขั้น · ความจำสองชนิด · ส่งต่อข้ามรุ่น
· หมู่บ้านมีบุคลิกสี่แบบ · ผู้กำกับภัยพิบัติที่เลือกจังหวะ
· แกนธรรม/อธรรมเปลี่ยน ฟ้า ทะเล ผืนดิน แสง เมฆ และเพลง

### มัธยม
สองภาค (มัธยม 120 วัน → ปีหนึ่ง 84 วัน) ใช้เอนจินเดียวกัน ต่างกันแค่ข้อมูล
· **ภูมิหลัง 6 แบบ** เลือกก่อนเปิดเทอม — ค่าตั้งต้น คนที่รู้จักอยู่แล้ว และกฎบางข้อของโลก
· **การเจอกันเป็นเรื่องบังเอิญ** ทุกคนมีตารางชีวิตที่ทอยใหม่ทุกวัน กระดานบอกได้แค่ว่า
  "มีคนอยู่" · เดินเข้าไปถึงจะรู้ว่าใคร · เจอที่เดิมบ่อยๆ ถึงจะรู้ตารางเขา
· **หน้าคนรู้จักขึ้นทีละคน** และครั้งแรกที่คุยกันมีบทของตัวเอง (`meet_*.ink`)
· ความสัมพันธ์สองแกน (สนิท/เชื่อใจ) · ไลน์ตอนกลางคืน · นัดซ้อน ·
  เหตุการณ์ลับหลัง + ระเบิดแบบ Tokimeki · คำโกหกที่โป๊ะ · การเลือกมีพยาน · คู่แข่ง
· ภาระสามอย่าง (การบ้าน สอบซ่อม งานกลุ่ม) · เกรดรายวิชา · กระดานประกาศผลหน้าห้อง ·
  งานใหญ่ของชมรม · ครูประจำชั้น · ทางบ้าน · ฝืนต่อทั้งที่หมดแรง · ของฝากรายคน
· มินิเกมเจ็ดตัว · ย้อนอ่านบท + เดินบทอัตโนมัติ · ฉากจบรายคน 8 ไฟล์ + ฉากจบของเรา
· **เรื่องหลักของเทอม "สมุดปกแดง" สามองก์** (วันที่ 6 · 51 · 84 · 110) — เบาะแสห้าชิ้นที่
  ได้มาจากความเชื่อใจ · คำให้การสี่แบบที่ปลายทางไปโผล่ในฉากจบของทุกคน
· **สีหน้าสิบแบบต่อคน** บทสั่งเองได้ (`~ feel`) และสลับคนพูดกลางฉากเหตุการณ์ได้ (`~ speak`)

## ของที่ยังไม่ได้ทำ (เรียงตามที่คิดว่าคุ้มสุด)

1. **มัธยม: บททักทายครั้งแรกยังมีกิ่งภูมิหลังไม่ครบทุกคู่**
   ตอนนี้ meet_ploy มีสามภูมิหลัง · meet_kanin สอง · meet_minta/palm หนึ่ง · tar/nun ยังไม่มี
   ทุกคู่ (6 คน × 6 ภูมิหลัง = 36) ไม่จำเป็นต้องมี แต่คู่ที่ *ควร* มีคือคู่ที่ภูมิหลัง
   ขัดกับตัวละครโดยตรง (ลูกครู × กนิน มีแล้ว · ซิ่ว × พลอย มีแล้ว)
2. **มัธยม: ชุมนุมของมหาลัยมีแค่อันเดียว**
   ถ้าจะให้ภาคปีหนึ่งลึกเท่ามัธยม ต้องมีอย่างน้อยสามชุมนุมและงานของตัวเอง
3. **ปฐมเทพ: สายจูงสามแบบ · ของเล่นในคอก · การเรียนแบบมีเงื่อนไข**
   ("กินเมื่อพลังต่ำกว่า 30%") — ตอนนี้น้ำหนักเป็นค่าเดียว ไม่ผูกกับสถานะ
4. **มัธยม: `s.rivals` / `s.gifted` ไม่ถูกล้างตอนขึ้นภาค** ตอนนี้ไม่มีผลเสีย
   (เพื่อนมัธยมไม่โผล่ที่มหาลัย และ `stepRivals` กรองตามภาคแล้ว) แต่ถ้าวันหนึ่ง
   ให้เพื่อนมัธยมโผล่ที่มหาลัยได้ ต้องกลับมาดูข้อนี้
5. **โมดูลที่ยังไม่มีเทสต์หน่วยตรงๆ** (มี `npm run balance` คุมทางอ้อม):
   `actions` `calendar` `chapter` `economy` `exam` `grades` `grooming`
   `homework` `schoolwork` `shop` `state`
6. **ปฐมเทพ: `npm run sim` รายงาน "สอนสำเร็จ N ท่า"** ตอนนี้ 14 ท่าแล้ว
   ถ้าวันไหนกลับไปเป็น 0 แปลว่าระบบลังเลพังอีกรอบ — เป็นสัญญาณเตือนที่ดีที่สุดที่มี

## รอบล่าสุด (16 ก.ย. 2026) — มัธยม: เกมนี้เกี่ยวกับอะไร และคนในเกมพูดเหมือนคนจริงหรือยัง

สี่เรื่องที่เจ้าของโปรเจกต์บอกหลังเล่นจริง: ภาพตัวละครดูเหมือนคนโรคจิต · ไม่มีจุดมุ่งหมาย
เนื้อเรื่องเบามาก · ทุกอย่างเป็นข้อความล้วน ไม่มีภาพไม่มีแถบบอกความคืบหน้า ·
คำพูดบางอย่างไม่เหมือนคนจริงพูดกัน

### 1. เนื้อเรื่องหลัก — "สมุดปกแดง" (`src/sim/plot.ts` · `story/mp_*.ink`)
สามองก์ตลอด 120 วัน ใช้ของที่เกมมีอยู่แล้วทั้งหมด ไม่ได้สร้างระบบใหม่ซ้อนเข้าไป
**จุดที่ตั้งใจที่สุดคือวันที่ 51** — ทุกคนถูกเรียกสอบสวนเป็นคนแรกเหมือนกัน แต่ด้วยเหตุผล
คนละอย่างสิ้นเชิงตามภูมิหลังทั้งหก ตรงนั้นคือจุดที่ภูมิหลังกลายเป็นเนื้อเรื่อง ไม่ใช่แค่ค่าตั้งต้น
**ความเชื่อใจกลายเป็นกลไกของเรื่อง** แต่ละคนถือเบาะแสคนละชิ้นและเล่าเมื่อไว้ใจถึงระดับเท่านั้น
ชิ้นที่ห้าค้นเองได้ที่ห้องสมุด (ทางเดียวที่ไม่ต้องพึ่งใคร แลกด้วยเวลาทั้งช่วง)
**ที่ประชุมกรรมการวันที่ 110** เปิดทางให้ต่างกันตามสองอย่างที่สะสมมาคนละทาง —
สิ่งที่เรารู้ กับ **คนที่ยอมยืนขึ้นด้วย** · ปลายทางทั้งสี่ไปโผล่ที่ฉากจบของทุกคน
**การ์ดจุดมุ่งหมายบนกระดาน** บอกตลอดว่ากำลังเดินไปหาอะไร ของเดิมไม่เคยบอกเลย

### 2. ภาพกับแถบบอกความคืบหน้า
- **ภาพตัวละครเปลี่ยนไปใช้ชิ้นส่วน CC0 ประกอบล่วงหน้า สิบสีหน้าต่อคน** (60 ไฟล์)
  สามอย่างที่ทำให้หน้าทุกคนดูเหมือนคนโรคจิต: หน้ากับคอคนละสีผิว · ม่านตาบานเย็น/ฟ้านีออน ·
  ไม่มีชั้นเงาเปลือกตา — แก้ครบทั้งสามใน `data/looks.json`
- **`~ feel(mood)` ให้บทสั่งสีหน้าเองได้** ไม่สั่งก็เดาจากคำในประโยคให้ (ของเดิมดูแค่
  เครื่องหมายท้ายประโยค = ทุกคำถามในเกมทำให้ตัวละครทำหน้าตึง)
- **`~ speak(charId)` สลับคนพูดกลางฉากเหตุการณ์ได้** ฉากเหตุการณ์ทุกฉากเคยเล่นด้วย
  `charId` เป็น null แปลว่าฉากที่มีตัวละครอยู่ด้วยจริงๆ (ที่ประชุมกรรมการ · ปัจฉิมนิเทศ)
  ขึ้นธงโรงเรียนแทนหน้าคนทั้งฉาก **และ `~ feel` ทุกบรรทัดในนั้นก็ไม่มีผลอะไรเลย**
- **แถบสองแกนในหน้าคนรู้จัก · หลอดค่าสถานะบนแถบบน · ชิปราคาเวลาบนปุ่มทุกปุ่ม**

### 3. คำพูด — ห้าเรื่องที่เจอตอนไล่อ่านบททั้ง 47 ไฟล์

| ที่เจอ | อาการ |
|---|---|
| **ฟองขวาในแชทเป็นคำพูดของอีกฝ่าย** | ทางเลือกติวเขียนเป็น `["มึงติวให้กูหน่อยดิ" — ติวให้]` ซึ่งเป็นสิ่งที่ *เขา* พูด แต่ฝั่ง UI เอาข้อความในวงเล็บไปทำฟองขวาเสมอ · **ในแชทเราจึงนั่งพิมพ์ขอให้ตัวเองติวตัวเอง** · ย้ายคำขอขึ้นไปเป็นข้อความที่เขาส่งมา แล้วทางเลือกกลายเป็นคำตอบของเราจริงๆ (6 ไฟล์) |
| **คำอธิบายบนปุ่มติดไปในฟองด้วย** | `["ดีใจด้วยนะ" — ยินดีกับเขาไปตรงๆ]` โผล่ในแชทเป็น «ดีใจด้วยนะ" — ยินดีกับเขาไปตรงๆ» พร้อมอัญประกาศค้างกลางประโยค · ตอนนี้ปุ่มโชว์เต็ม ฟองโชว์แต่สิ่งที่เราพิมพ์ |
| **สรรพนามหลุด** | บล็อกที่ copy ข้ามไฟล์กันมาเรียกพลอย มินตรา นุ่น ว่า "เขา" กลางบทที่เรียก "เธอ" ตลอด |
| **หกคนตอบกลับด้วยประโยคเดียวกันเป๊ะ** | `"หายดีหรือยัง" เขาถามก่อนอย่างอื่นทั้งหมด` เหมือนกันทั้งหกไฟล์ · เช่นเดียวกับบล็อกที่เขาหยิบเรื่องเก่ามาพูด · เขียนใหม่ให้เป็นเสียงของแต่ละคน |
| **ต้าร์เรียกตัวเองว่า "ผม" หนึ่งบรรทัด** | ทั้งเกมเขาเรียกตัวเองว่า "พี่" |

**เติมสีหน้าเข้าไปใน `nun.ink` `tar.ink` `palm.ink` `meet_tar.ink` และฉากจบทั้งแปดไฟล์**
พร้อมเกลาคำพูดในจังหวะเดียวกัน (เติมคำลงท้าย ตัดประโยคให้สั้นลง ใส่ท่าทางระหว่างพูด)

### 4. บั๊กที่เจอระหว่างทาง

| ที่เจอ | ความหนัก | อาการ |
|---|---|---|
| **เหตุการณ์ของเรื่องหลักทั้งสี่ไม่มีฉากหลัง** | หนัก | เพิ่ง `npm run unit` ถึงรู้ — องก์ทั้งสามและฉากที่ครูเรียกคุยรอบสองเล่นบนจอดำเปล่า · `EVENT_PLACE` ไม่ได้ถูกเติมตอนเพิ่มเหตุการณ์ |
| **ที่ประชุมกรรมการวนลูปไม่รู้จบ** | หนัก | ทางเลือกสองอันที่บอกใบ้แล้ววนกลับมาที่ `choose` เป็น `+` (เลือกซ้ำได้ไม่จำกัด) · `npm run story` เดินไป 13,546 บรรทัด 3,138 ทางเลือกแล้วยังไม่จบ · เปลี่ยนเป็น `*` คำใบ้อ่านรอบเดียวก็พอ |
| **`~ speak()` วางผิดบรรทัดหนึ่งบรรทัด** | เบา | ประโยค "พวกเธอมาทำอะไรกันที่นี่" เป็นของประธานกรรมการ แต่หน้าบนจอสลับไปเป็นเพื่อนที่เพิ่งเดินเข้ามาแล้ว · เจอตอนเขียนเทสต์ภาพของฉากนี้ |
| **คำพูดของเพื่อนที่ยืนขึ้นด้วยบอกเพศ** | เบา | ลงท้ายด้วย "ค่ะ" ทั้งที่คนที่ยืนขึ้นเป็นใครก็ได้ในหกคน · เขียนใหม่ให้ละประธานแบบที่คนพูดกันจริง |
| **ตัวนับ "เรื่องหลักเดินจริงไหม" โกหก** | กลาง | วัดที่ `plotStage >= 3` ซึ่ง `settleVerdict` ตั้งเป็น 4 ทุกรอบที่ให้การ = จริงเสมอ · เปลี่ยนไปวัดที่จำนวนเบาะแส ตอนนี้รายงานตามจริง 9/25 รอบ |

**ตัวนับใหม่ที่เพิ่มเข้าไป** `npm run story` ล้มถ้ามีสีหน้าไหนไม่มีบทไหนเรียกเลย
(ภาพหกสิบใบถูกสร้างขึ้นมาแล้วต้องได้ออกจอทั้งหมด) และเตือนถ้าบทของใครสั่งสีหน้าน้อยกว่าสามแบบ
— นับเฉพาะบทที่มีหน้าคนอยู่บนจอจริงๆ ฉากเหตุการณ์เป็นเสียงบรรยาย สั่งไปก็ไม่มีอะไรเปลี่ยน

**ตัววัดหลังแก้** เล่นจนจบ 12 รอบด้วยบทจริงไม่มีรอบไหนพัง · เหตุการณ์ตามปฏิทินเล่นจริง 28/28 ·
สะพาน ink→TS ถูกเรียกครบ 43/43 · สีหน้าถูกสั่งครบ 10/10 · ธง 185/185 มีคนอ่าน ·
เทสต์ภาพ 32 อันผ่านหมด

## รอบล่าสุด (16 ก.ย. 2026) — หน้าตาของคนกับสัตว์ และสัดส่วนที่ผิดมาตลอด

ทั้งหมดนี้หาเจอเพราะ **เพิ่มภาพระยะใกล้ของชาวบ้าน** แล้ววัดว่าคนกินพื้นที่กี่เปอร์เซ็นต์ของเฟรม

| สิ่งที่วัดได้ | ก่อน | หลัง |
|---|---|---|
| ความสูงชาวบ้าน (กระท่อมสูง 0.86) | 0.87 — สูงเท่าหลังคาบ้านตัวเอง | 0.28 |
| ชาวบ้านเอียงตามพื้น | 33° | 14° |
| สีเสื้อในหมู่บ้าน | เหมือนกันหมดทุกคน | หกสี |
| คนกินพื้นที่ในภาพระยะใกล้ | 0% (ยืนอยู่ในกระท่อม) | 0.96% |
| สัตว์กินพื้นที่ในภาพระยะใกล้ | 0% (ต้นไม้บังมิด) | 17.9% |
| เวลาต่อเฟรมที่ 96 คน เทียบกับแบบกรวย | +18% | +13% |

- **`world.coverage(obj)`** — เครื่องมือใหม่ วาดสองเฟรมเทียบกัน (มีของชิ้นนั้น/ไม่มี) แล้วนับพิกเซล
  ที่ต่าง เทสต์ภาพระยะใกล้ทุกอันมีตัวเลขนี้คู่กับภาพแล้ว เพราะ "ภาพเหมือนรอบที่แล้ว"
  ไม่ได้แปลว่า "ของที่ตั้งใจถ่ายอยู่ในภาพ"
- **สัตว์เปลี่ยนสีเป็นน้ำตาลอุ่นแล้ว** (`creature.tint` ใน `data/models.json`)
  ของเดิมเป็นวัวช็อกโกแลตเข้มซึ่งบนทุ่งหญ้าอ่านออกมาเป็นเงาดำก้อนเดียว
- **หญ้าเป็นใบเรียวปลายและรับแดดเหมือนพื้นแล้ว** ของเดิมเป็นสี่เหลี่ยมผืนผ้าเส้นตั้งฉากนอน
  ซึ่งตอนซูมเข้าใกล้อ่านออกมาเป็นการ์ดกระดาษสีเข้มปักดินเต็มเนิน
- **โมเดลของ Kenney สามชิ้น (ต้นไม้ หิน กระท่อม) ถูกลบออก** ไม่มีใครเรียกตั้งแต่เปลี่ยนไป
  สร้างจากโค้ดใน `buildings.ts` — ทะเบียนที่ชี้ไปยังไฟล์ที่ไม่มีใครเรียกคือที่ที่เสียเวลาที่สุด

## รอบล่าสุด (16 ก.ย. 2026) — มัธยม: ของที่เขียนไว้แล้วแต่ไม่เคยโผล่บนจอ

- **เหตุการณ์ 10 จาก 18 บทเล่นบนจอดำเปล่า** `playInk()` ใส่ฉากหลังให้เฉพาะบทที่ชื่อ
  บังเอิญตรงกับชื่อฉากเท่านั้น · บทที่ไม่มีฉาก ได้แก่ **ฉากแรกสุดที่ผู้เล่นเห็นตอนเปิดเกม**
  และเหตุการณ์หลักทั้งสี่ของภาคมหาลัย · ตอนนี้มี `EVENT_PLACE` ให้ยืมภาพของ
  *ที่ที่บทนั้นเกิดขึ้น* และ `src/ui/backdrop.test.ts` ล้มถ้ามีบทไหนไม่มีฉาก
- **หลอดใต้ชิปบนแถบบนมองไม่เห็นมาตลอด** มีกฎ CSS เฉพาะ `.chip.energy i`
  ทั้งที่ชิปค่าสถานะ ครู และหนี้การนอน ส่ง `<i>` ที่คำนวณเปอร์เซ็นต์มาแล้วออกมาเหมือนกัน
  `<i>` เปล่าที่ไม่มีกฎคือ inline สูงศูนย์ ไม่มีสีพื้น
- **ค่าสถานะห้าตัวไม่เคยกระพริบเลยสักครั้ง** ตัวจับความเปลี่ยนแปลงแกะตัวเลขจากข้อความ
  แต่ชิปค่าสถานะโชว์เป็น *ชื่อระดับ* (`"พอไหว"`) · `Number("")` คืน **0 ไม่ใช่ NaN**
  ทั้งห้าใบจึงอ่านได้ 0 เท่ากันตลอด แล้วเงื่อนไข "ค่าเดิมเท่าค่าใหม่" เป็นจริงทุกครั้ง
  ตอนนี้อ่านจาก `data-k` / `data-v` ที่ชิปประกาศเอง
- **หลอดค่าสถานะวัดจากศูนย์ถึงตำนาน ไม่ใช่วัดภายในระดับ** ชิปห้าใบนี้อยู่ติดกันเพื่อให้
  เทียบกันเอง ถ้าแต่ละใบวัดจากฐานคนละอัน ค่าที่เพิ่งขึ้นระดับใหม่จะดูแย่กว่าค่าที่ค้าง
  อยู่ท้ายระดับเดิม · "อีกเท่าไหร่ถึงระดับถัดไป" ย้ายไปอยู่ใน tooltip
- **ภาพฉากในกล่องบทสูงขึ้นบนจอมือถือแนวตั้ง** (`aspect-ratio` 400/215 เมื่อจอสูงเกิน 740px)
  ของเดิมเหนือภาพเป็นที่ว่างเกือบครึ่งจอทุกฉาก · ห้ามใช้ `vh` ตรงๆ (เหตุผลอยู่ใน CLAUDE.md)
- ภาพ `hallway-*.jpg` สี่ใบที่ไม่เคยถูกใช้เลย ตอนนี้เป็นฉากของวันสมัครชมรม

**เรื่อง "ทางบ้าน รายงาน `คนที่ส่งให้ 0`" ที่ค้างไว้: ไม่ใช่บั๊ก** ตัวเลขถูกแล้ว
มันคือ *ระดับบ้านตึงตอนจบ* ของคนที่ส่งเงินให้ (0) เทียบกับคนที่เก็บไว้เอง (6)
ป้ายกำกับเขียนกำกวมจนอ่านเหมือนเป็นจำนวนคน ตอนนี้แก้ป้ายแล้ว

## รอบตรวจละเอียด (16 ก.ย. 2026) — บั๊กที่เจอหลังยกเครื่องเสร็จ

เขียนเครื่องมือใหม่สามตัวก่อน แล้วปล่อยให้มันหาเอง: `npm run audit` (ความสมเหตุสมผลข้ามข้อมูล) ·
`npm run playthrough` (เล่นจนจบด้วยบทจริง) · เทสต์ภาพที่ **เล่นมินิเกมทั้งเจ็ดจนจบ**

| ที่เจอ | ความหนัก | อาการ |
|---|---|---|
| **`story/chat_ploy.ink` พังมาหลาย commit แล้ว** | หนักที่สุด | ทางเลือกหนึ่งอันถูกวางทับบรรทัดคอมเมนต์ แล้ว `-> DONE` ของมันปิดบทตั้งแต่บรรทัดที่ 10 · **บทไลน์ของพลอย 169 บรรทัดเหลือให้เล่นจริง 22 บรรทัด ไม่มีตัวเลือกสักอัน** ทุกคืนที่เธอทักมาจบเหมือนกันหมด · แก้แล้วเดินได้ 956 บรรทัด 234 ทางเลือก |
| **เทสต์ที่ปล่อยให้ข้อบนผ่านมาได้** | หนัก | `storywalk.ts` นับ `bad++` *ก่อน* push ปัญหาสองข้อสุดท้าย ตัวตรวจ "บทแชทไม่มีทางชวนนัด" จึงพิมพ์คำเตือนแล้วเทสต์ผ่าน |
| **`routine` ห้าช่องชี้ไปยังที่ที่ปิดในช่วงนั้น** | กลาง | พลอย/หลังเลิกเรียน→ห้องเรียน · กนิน/เช้า→สนามหลังตึก · มินตรา/กลางวัน→ห้องดนตรี ฯลฯ น้ำหนักตรงนั้นตายสนิท ตัวละครหายไปเฉยๆ ในสัดส่วนนั้น |
| **เดินสวนกันให้ความสนิทฟรีเกินไป** | กลาง | ทอยทุกช่วงเวลา = วันละ 4 ครั้ง × 120 วัน ได้ราว 46 แต้มต่อเทอม พอๆ กับไปนั่งคุยจริงทั้งเทอมโดยไม่เสียเวลาสักช่วง · ตอนนี้วันละครั้ง |
| **ค้างอยู่ในห้องเดิมหลังเวลาเดิน** | กลาง | ทำกิจกรรมเสร็จแล้วยังอยู่หน้า "ในที่นั้น" ของช่วงเวลาใหม่ — ข้ามขั้นตอน "เลือกว่าจะไปไหน" ซึ่งเป็นการตัดสินใจหลักของเกม |
| **รับนัดข้ามภาคแล้วผิดนัดแน่นอน 100%** | กลาง | เพื่อนมัธยมทักไลน์มาตอนอยู่มหาลัยได้ (ตั้งใจ) แต่ถ้ารับนัด พรุ่งนี้เขาไม่อยู่ที่ไหนเลย โดนหักฟรี · ตอนนี้ `tomorrowSchool` คลุมเงื่อนไขข้ามภาคด้วย |
| **ออกค่ายอาสา = เกมหลบครู** | เบา | ชมรมบำเพ็ญประโยชน์เป็นชมรมเดียวที่ไม่มีมินิเกมของตัวเอง วันงานใหญ่เลยไปยืมเกมหลบครูมาใช้ · ตอนนี้ใช้เกมจัดของ |
| **ชื่อคู่แข่งเขียนตรงๆ ในบท 12 ที่** | เบา | เปลี่ยนชื่อใน characters.json แล้วบทยังเรียกชื่อเดิม · ตอนนี้ใช้ `{rivalName()}` ทั้งหมด |
| **สะพาน ink 6 ตัวไม่มีบทไหนเรียก** | เบา | `myRank` `trustOf` `toldAlready` `gainTrust` `rivalName` = ระบบที่มีอยู่แต่ไม่เคยโผล่ในบทสนทนา · เขียนบทให้ครบแล้ว และ `metDays` ที่เพิ่งเพิ่มแล้วไม่ได้ใช้ถูกลบทิ้ง |
| **`hintAtRank` เป็นเงื่อนไขที่ไม่เคยกั้นอะไร** | เบา | เชื่อมด้วย OR กับ "เคยเจอกันแล้ว" ซึ่งเป็นจริงเสมอเมื่อสะสม habit ครบ · ลบทิ้ง |
| **บททักทาย 3 เส้นทางจบโดยไม่เคยบอกชื่อ** | เบา | คุยจบแล้วยังไม่รู้ว่าคุยกับใคร ทั้งที่หน้าคนรู้จักขึ้นชื่อเขาไปแล้ว — สองหน้าจอขัดกันเอง |
| **`serve` สุ่มกองของใหม่ทุกครั้งที่จัดเสร็จหนึ่งชิ้น** | เบา | ของกระโดดสลับที่ทุกครั้งที่กด กลายเป็นการวัดสายตาแทนการจัดของ |
| **นาฬิกาในมินิเกมเดินต่อตอนสลับไปแอปอื่น** | กลาง (สำคัญกับ iPad) | เบราว์เซอร์หยุด `requestAnimationFrame` ตอนแท็บไม่ได้อยู่หน้าจอ แต่ `performance.now()` ยังเดิน · สลับแอปสองวินาทีกลางเกมท่องจำ กลับมาอีกทีคำหายไปแล้ว ข้อสอบหมดเวลาแล้ว เวลาจัดของหมดแล้ว โดยไม่ได้ทำอะไรผิด · ตอนนี้ทุกมินิเกมใช้นาฬิกาที่หยุดเดินตอนไม่ได้มอง |
| **CSS ที่ iPad รุ่นเก่าอาจไม่รองรับ** | เบา | `color-mix()` ไม่มีสีสำรอง (ได้ขอบสี currentColor) · `backdrop-filter` ไม่มี `-webkit-` |

**ตัววัดหลังแก้** ไปดักที่ที่เขาอยู่บ่อยที่สุดเจอ 35–47% ของวัน (เดิมบางช่องอ่านได้ 76%
เพราะนับที่ที่เข้าไม่ได้รวมด้วย) · เล่นจนจบ 12 รอบด้วยบทจริงไม่มีรอบไหนพัง ·
เหตุการณ์ตามปฏิทินถูกเล่นจริงครบ 24/24 · สะพาน ink→TS ถูกเรียกจริงครบทุกตัว

## สามบั๊กที่เพิ่งแก้ และเป็นแบบที่หาเองไม่เจอถ้าไม่วัด

- **สัตว์ไม่เคยลงมือทำอะไรได้เลย** `startAction()` เรียก `chooseAction()` ใหม่ทุก tick
  แล้วรีเซ็ตเวลาลังเลทุกครั้งที่ได้คนละท่า ซึ่งเกิดเกือบทุกครั้ง
  เดิน 600 tick แล้ว `c.act` ไม่เคยถูกตั้งเลย · ไม่มี error ให้เห็นสักบรรทัด
  แก้แล้วผู้เล่นจำลองชมได้จาก 9 เป็น 119 ครั้งต่อโลก
- **กระดานประกาศผลอ่านแรงกดดันที่ถูกล้างทิ้งไปแล้ว** ชีวิตเพื่อนพัง = คะแนนเขาดีขึ้น
- **ชมรมมัธยมอยู่ที่สถานที่มัธยม แต่มหาลัยเปิดให้สมัคร** = โทษฟรีที่ทำอะไรไม่ได้

บทเรียนเดียวกันทั้งสามข้อ: **ระบบที่ไม่มีตัวนับ ตายแล้วไม่มีใครรู้**
ทุกระบบใหม่ต้องมีตัวนับในเทสต์สมดุล และมีคำเตือนที่ยิงเมื่อตัวนับเป็นศูนย์
