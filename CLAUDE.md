# มัธยม (mattayom)

เกมชีวิตในโรงเรียนไทย — Persona 5 + Bully + dating sim
ตอบเป็นภาษาไทย อธิบายโค้ดเป็นขั้นตอน เจ้าของโปรเจกต์ไม่ได้มาสายเขียนโค้ด
พูดตรง ชี้ข้อผิดพลาดได้เลย ไม่ต้องประคอง

**เซสชันแรก อ่าน `HANDOFF.md` ก่อน** (เซสชันถัดไปไม่ต้อง)

## คำสั่ง

```bash
npm run dev       # dev server พอร์ต 5174
npm run lint:ink  # ตรวจว่าบททุกไฟล์คอมไพล์ผ่าน
npm run story     # เดินบททุกเส้นทางแบบ headless — จับทางตันและทางที่ล็อกไว้แล้วเปิดไม่ออก
npm run balance   # จำลองเล่นจนจบเทอม 5 กลยุทธ์ × 5 seed — จับค่าสถานะเฟ้อ ระบบแรง และฝ่ายปกครองที่ไม่ทำงาน
npm run check     # tsc --noEmit
```

## เกมนี้มีสองภาค

**มัธยม (`school`) แล้วต่อด้วยปีหนึ่ง (`uni`)** ใช้เอนจินเดียวกันทุกอย่าง — ปฏิทิน ช่วงเวลา
ค่าสถานะ ไลน์ การบ้าน สอบ — ต่างกันแค่ *ข้อมูล* ซึ่งเป็นเหตุผลที่สถาปัตยกรรมนี้ถูกวางไว้แบบนี้

ทุกชิ้นใน `data/locations.json` `data/events.json` `data/characters.json` มีฟิลด์ `chapter`
**ไม่ระบุ = ของมัธยม** เพื่อให้ข้อมูลเดิมใช้ต่อได้โดยไม่ต้องแก้ทุกบรรทัด
เพิ่มของใหม่ให้ปีหนึ่งต้องใส่ `"chapter": "uni"` ทุกครั้ง ไม่งั้นมันจะไปโผล่ในมัธยม

**เพื่อนมัธยมไม่โผล่มาที่มหาลัย แต่ยังทักไลน์มาได้** — `whoIsAt()` กรองตามภาค
ส่วนระบบไลน์ไม่กรอง นี่คือเรื่องตั้งใจ ความสัมพันธ์ถูกหั่นเหลือ 45% ตอนขึ้นภาค
คนที่ไม่ได้สนิทจริงจะหลุดต่ำกว่าเกณฑ์และหายไปเอง

## หลักการเดียวที่ทุกอย่างขึ้นกับมัน

**ink เป็นเจ้าของคำพูด TypeScript เป็นเจ้าของตัวเลข**

ถ้าต้องแก้ไฟล์ `.ts` เพื่อเพิ่มบทสนทนา แปลว่ากำลังจะทำผิดทาง — หยุดแล้วบอกก่อน
เพิ่มตัวละคร = เพิ่ม object ใน `data/characters.json` + สร้าง `story/<ชื่อ>.ink`
เพิ่มเหตุการณ์ตามปฏิทิน = เพิ่ม object ใน `data/events.json` + สร้าง `story/ev_<ชื่อ>.ink`

## กติกา

**ค่าสถานะ ความสัมพันธ์ และเงิน เปลี่ยนผ่าน external function เท่านั้น**
ใช้ `~ gainAffinity("ploy", 2)` ไม่ใช่ `~ affinity = affinity + 2`
ตัวแปรใน ink มีไว้ **อ่าน** เป็นเงื่อนไข เช่น `{mind >= 6}` เพราะ TS เป็นเจ้าของตัวเลข
ถ้าเขียนทับใน ink ตรงๆ ค่าจะไม่ถูกบันทึกลงเซฟ
รายการตัวแปรและ external ทั้งหมดอยู่ใน `story/_shared.ink` คู่กับ `src/story/bridge.ts`

**ความสัมพันธ์ต้องเปลี่ยนผ่าน `changeAffinity()` ใน `src/sim/bonds.ts` เท่านั้น**
ห้ามเขียน `s.affinity[id] += n` ตรงๆ ไม่งั้นระลอกที่ไปถึงคนอื่นจะหายไปเงียบๆ เฉพาะทางนั้น
หัวหน้าห้องกับเด็กหลังห้องไม่ถูกกัน (`bonds` ใน characters.json) สนิทฝั่งหนึ่งมากขึ้น อีกฝั่งห่างออกไป
ซึ่งเป็นการบังคับให้ผู้เล่น *เลือก* ไม่ใช่การลงโทษ

**`takeSide()` ใช้ได้ครั้งเดียวทั้งเทอม** บทระดับ 9 ของแต่ละคนมีจุดนี้ เลือกแล้วอีกฝั่งปิดถาวร
ทางเลือกนั้นต้องกั้นด้วย `{sideTaken() == 0 || sidedWith("ชื่อ") == 1}` เสมอ

**ตัวเลขสมดุลทั้งหมดอยู่ใน `data/*.json`** — game, locations, characters, clubs, shop, events
**ภาระสามอย่างที่เกมยัดใส่มือผู้เล่น ไม่ใช่สิ่งที่เขาเลือกทำเอง**
การบ้าน (ทุกวันเรียน) · สอบซ่อม (หลังประกาศผลสอบ) · งานกลุ่ม (จับคู่ให้ มีเส้นตาย)
ทั้งสามโผล่เป็นการ์ดบนสุดของกระดาน เพราะมันมาก่อนของที่เลือกทำได้
**งานกลุ่มจับคู่กับคนที่สนิทน้อยที่สุดเสมอ** ซึ่งเป็นประเด็นของระบบ — บังคับให้รู้จัก
คนที่ไม่ได้ตั้งใจจะรู้จัก แทนที่จะเทเวลาให้คนเดียวทั้งเทอม

**การบ้านอยู่ในทางเดินของ `advance()`** ครูสั่งทุกวันเปิดเรียนและเก็บเช้าวันเปิดเรียนถัดไป
ทำให้ช่วง "กลางคืน" ที่เคยเกือบฟรีมีของที่ *ต้อง* ทำ ไม่ใช่ของที่เลือกทำ
ระวังตอนปรับ `homework.behaviourPenalty` — มันซ้อนกับการโดนจับของฝ่ายปกครอง
ตอนนี้เด็กหลังห้องที่ไม่ส่งการบ้านเลยจบเทอมด้วยความประพฤติ 17 และโดนห้ามเข้าที่เสี่ยง 254 ช่วง
ซึ่งสมจริงแต่แรง ถ้ารู้สึกว่ามากไปให้ปรับที่ `behaviourPenalty` ก่อน `recoverPerWeek`

**หลังแก้ `data/game.json` ให้รัน `npm run balance`** ห้าอย่างที่ต้องดู:
ไม่มีค่าสถานะไหนเต็มก่อน 1 ใน 4 ของเทอม · แรงลงต่ำกว่าขีดล่างอย่างน้อยหนึ่งกลยุทธ์ ·
เล่นจริงจังต้องได้ผลต่างจากไม่ทำอะไรเลยอย่างชัดเจน · ต้องมีคนโดนฝ่ายปกครองจับบ้าง ·
ฝีมือมินิเกมต้องขยับคะแนนปลายทางได้อย่างน้อย 3 คะแนน

**`npm run balance` เล่น 5 กลยุทธ์ × 5 seed** กลยุทธ์ "เด็กหลังห้อง" มีไว้เพื่อให้
`rollCatch` `escapeCatch` และ `inTrouble` ได้ทำงาน ไม่ใช่เพื่อวัดสมดุลของการเล่นแบบนั้น
ถ้าลบกลยุทธ์นี้ออก ฝ่ายปกครองทั้งระบบจะกลับไปไม่มีเทสต์แตะอีก

**ไลน์ตอนกลางคืน: `src/sim/chat.ts` เป็นเจ้าของ "ใครทักเมื่อไหร่ นัดแล้วได้เสียอะไร"
ส่วนคำพูดอยู่ใน `story/chat_<ชื่อ>.ink` ทั้งหมด** ไม่มีข้อความบทสนทนาในไฟล์ `.ts` สักบรรทัด
เพิ่มตัวละครใหม่ต้องสร้าง `story/chat_<id>.ink` ด้วย ไม่งั้นพอเขาทักมาจะเปิดอ่านไม่ได้
(main.ts กันไว้แล้วไม่ให้เกมค้าง แต่ผู้เล่นจะเห็นข้อความว่ายังไม่มีบท)

**หลังแก้ไฟล์ `.ink` ให้รัน `npm run lint:ink` แล้วตามด้วย `npm run story`**
lint บอกแค่ว่าคอมไพล์ผ่าน · story บอกว่าเดินได้จริงและไม่มีทางตัน
`npm run check` จับ error ในบทไม่ได้เลยทั้งสองอย่าง

## กับดัก ink ที่เคยเสียเวลามาแล้ว

**`story/_shared.ink` ห้ามมี knot (`===`) หรือ function**
เนื้อหาถูกแทรกไว้ด้านบนของไฟล์บท ถ้ามี knot ทุกอย่างที่ตามมาจะตกเข้าไปอยู่ในนั้น
แล้วได้ error ที่อ่านไม่รู้เรื่องว่า "Functions may not contain diverts"

**ชื่อ knot และชื่อตัวแปรต้องเป็น ASCII** ทดสอบแล้วว่าชื่อภาษาไทยคอมไพล์ไม่ผ่าน
(ตัว *ข้อความ* ภาษาไทยไม่มีปัญหาเลย ทั้งคอมไพเลอร์และ runtime)

**ink ประกอบชื่อ knot จากตัวแปรไม่ได้** `-> rank_{affinity}` ใช้ไม่ได้ ต้องเขียนเป็น:
```
{
    - rank < 1:  -> rank_0
    - rank < 3:  -> rank_1
    - else:      -> rank_2
}
```

**เลขบรรทัดใน error ของ lint:ink ไม่ตรงกับไฟล์จริง** เพราะ `_shared.ink` ถูกแทรกด้านบน
ให้ลบจำนวนบรรทัดของ `_shared.ink` ออกก่อนไปหาตำแหน่ง

**ทางเลือกที่ล็อกด้วยค่าสถานะจะหายไปเฉยๆ ผู้เล่นไม่มีทางรู้ว่าพลาดอะไร**
วิธีที่ใช้ในโปรเจกต์นี้คือใส่ทางเลือกคู่กัน แล้วให้ฝั่งที่ยังไม่ถึงเรียก `~ setHint("...")`
UI จะโชว์เป็นบรรทัดจางใต้ตัวเลือกเอง

**`{~ a|b|c}` (shuffle) ทำให้เล่นซ้ำได้คนละเส้นทาง** `npm run story` ตรวจจับเองแล้วสลับไป
เดินแบบสุ่ม 40 รอบแทน BFS ถ้าเขียนบทสุ่มใหม่ไม่ต้องทำอะไรเพิ่ม

## กับดักอื่น

- **ทางเลือกที่ชวนนัดต้องกั้นด้วย `{tomorrowSchool}` เสมอ** ตัวละครนัดเจอกันที่โรงเรียน
  ถ้าพรุ่งนี้เป็นเสาร์อาทิตย์หรือวันหยุด สถานที่นั้นเข้าไม่ได้ ผู้เล่นจะผิดนัดทั้งที่ไม่ได้ทำอะไรผิด
  แล้วโดนหักความสัมพันธ์ฟรีๆ `npm run story` จับให้แล้วว่าบทแชทไฟล์ไหนไม่มีเส้นทางชวนนัดเลย
  และมีโปรไฟล์ "สนิทแต่พรุ่งนี้โรงเรียนปิด" ไว้เดินฝั่งที่โรงเรียนปิดโดยเฉพาะ
- **`window.__mattayom` มีตอน dev** = { s, render, next, save, openChatList, rollChat,
  playEpilogues, epilogues } — `playEpilogues()` ใช้ดูฉากจบโดยไม่ต้องเล่นให้จบเทอมจริง
  เกมเดินทีละช่วงเวลา ถ้าต้องเล่นจริงกว่าจะถึงจุดที่อยากดูจะดีบั๊กไม่ไหว
- **เทสต์สมดุลเคยใช้ `rnd = () => 0.5` คงที่ แล้วปิดทั้งระบบโดยไม่มีใครรู้**
  โอกาสโดนจับสูงสุดในเกมคือ 0.34 (โดดเรียน) 0.22 (สนามหลังตึก) 0.16 (ร้านเกม)
  ต่ำกว่า 0.5 หมด `rnd() > chance` จึงเป็นจริงทุกครั้ง รายงานขึ้น "โดนจับ 0 ครั้ง"
  ทุกกลยุทธ์แบบที่เป็นอย่างอื่นไม่ได้ ตอนนี้ใช้ `mulberry32` ใน `src/core/rng.ts` แทน
  **ค่าคงที่ที่เลือกมาเพื่อให้ผลซ้ำได้ ต้องเช็กเสมอว่ามันไม่ได้ตกนอกช่วงที่ระบบจะทำงาน**
- **มินิเกมต้องถูกส่งคะแนนเข้าเทสต์ด้วย** `takeExam(s, id)` มี default `quizScore = 0.5`
  เทสต์เดิมไม่ส่งค่าเข้าไป ตัวคูณ 0.72–1.28 จึงไม่เคยถูกแตะที่ปลายทั้งสองข้าง
  เช่นเดียวกับ `doClubActivity(s, times, mult)` ที่เคยส่ง `times = 0` ตลอดจนผลตอบแทนไม่เคยลดหลั่น
- **เซฟรุ่นเก่าใช้ต่อไม่ได้** `SAVE_VERSION` ใน `src/sim/state.ts` ต้องบวกขึ้นทุกครั้งที่
  เปลี่ยนโครง `GameState` ไม่งั้นเซฟเก่าจะโหลดมาแล้วพังกลางทาง
- **`--ink` ในไฟล์ CSS นี้คือสีตัวอักษร** (ตรงข้ามกับ genesis ที่ `--ink` คือพื้นหลัง) ระวังสลับ
- **ชื่อคลาสสั้นๆ ชนกันง่ายมากในไฟล์ CSS เดียว** เคยตั้งคลาส `next` ให้จุดบอกช่วงเวลา
  แล้วไปโดนกฎของปุ่ม `.next` (`min-height:40px`) จุดเลยกลายเป็นกล่องสูง 40px
  คลาสที่เป็น "สถานะ" ให้ขึ้นต้นด้วย `is-` เสมอ
- **`#scene` เป็น `position:fixed` เต็มจอ** เนื้อหาข้างในต้องคุมความกว้างเองด้วย `max-width:640px`
  ไม่งั้นบนจอ Mac ข้อความจะลากยาวข้ามจนอ่านยาก (ตัวเกมคุมด้วย `#app` แต่ `#scene` อยู่นอก `#app`)
- **id ของ gradient ใน SVG ต้องสุ่มทุกครั้ง ห้ามใช้ตัวนับหรือชื่อคงที่**
  เคยใช้ `uid++` ในภาพตัวละคร และ `id="g"` เหมือนกันหมดในฉากหลัง ผลคือพอมีหลายภาพในหน้าเดียว
  `url(#g)` จะไปหยิบของภาพแรกในเอกสารเสมอ ภาพหลังๆ พื้นหลังหายหรือได้สีของภาพอื่น
  **ไม่มี error ให้เห็นเลย** ตรวจได้ด้วยการหา id ซ้ำในเอกสาร ตอนนี้ทั้งสองไฟล์ใช้ id สุ่มแล้ว
- **เพิ่มสถานที่หรือตัวละครใหม่ต้องเพิ่มสามอย่างเสมอ** ไอคอนใน `icons.ts` · ฉากใน `backdrop.ts` ·
  ทรงผมใน `LOOKS` ของ `portrait.ts` ขาดอันไหนจะได้ของสำรองเงียบๆ โดยไม่มี error
  (บ้าน · ฉากบ้าน · ภาพเสาธง) ตอนเพิ่มสถานที่ของมหาลัยเจอครบทั้งสามอย่างในรอบเดียว
- **ภาพตัวละครเป็น SVG ที่สร้างจากโค้ดใน `src/ui/portrait.ts`** ไม่ใช่ไฟล์รูป
  สไตล์อนิเมะ: ตาใหญ่มีม่านตาไล่สีและไฮไลต์สองจุด · ขนตาบนหนาสะบัดหางตา · ผมมีแถบแสง · แก้มมีสีเลือด
  **ชั้นของภาพสำคัญมาก** ผมหลัง → ชุด → หน้า → ตา/ปาก → ผมกระหม่อม → หน้าม้า
  วางผมหน้าไว้ก่อนหน้าทีเดียวผมจะทับหน้าทั้งใบ และถ้าไม่มีชั้นกระหม่อม หนังหัวจะโผล่เหนือหน้าม้า
  พื้นหลังการ์ดต้องสว่างกว่าสีผมเสมอ ไม่งั้นผมจมหายไปกับพื้น
  เพิ่มตัวละครใหม่ต้องเพิ่มทรงผมใน `LOOKS` ด้วย ไม่งั้นจะได้ภาพเสาธงแทนหน้าคน
- **`#sceneBg` ใช้ภาพฉาก 400x150 อันเดียวกับการ์ดสถานที่** ถ้าบังคับความสูงเป็น vh แล้ว slice
  มันจะถูกขยายกว่า 5 เท่าจนเห็นแค่ 40% ของภาพ ต้องคง `aspect-ratio:400/150` ไว้เสมอ
- **ทางเลือกทุกอันต้องมีคนอ่านธงของมัน** `npm run story` นับ `setFlag(...)` เทียบกับ
  `hasFlag(...)` ในบทและ `s.flags[...]` ในโค้ด **ถ้ามีธงที่ไม่มีใครอ่าน เทสต์จะล้ม**
  เคยมีธง 79 อันถูกตั้งแต่อ่านแค่ 34 แปลว่าทางเลือก 45 อันไม่ส่งผลอะไรเลยทั้งเกม
  ที่สำหรับอ่านธงพวกนี้คือ `story/epi_*.ink` (ฉากจบรายคน)
- **ฉากจบรายคนไม่มีทางเลือกสักอัน** การเดินบทแบบปกติจึงแตะได้แค่เส้นทาง "ไม่มีธงเลย"
  `npm run story` เลยเดินฉากจบสองรอบเทียบกัน (ไม่ตัดสินใจอะไรเลย vs ตัดสินใจครบ)
  ถ้าสองรอบยาวพอๆ กัน แปลว่าเงื่อนไขข้างในสะกดชื่อธงผิดแล้วเงียบไป
- **เพิ่มตัวละครใหม่ต้องเพิ่ม `epilogue` ใน `characters.json` และไฟล์ `story/epi_<id>.ink` ด้วย**
  ถ้าไม่มีไฟล์ ฉากจบของคนนั้นจะพังตอนเล่นจริง ไม่ใช่ตอนเทสต์
- **แถบภาพฉากต้องอยู่ใน flow เหนือกล่องบท ห้ามเป็น `position:absolute` ติดขอบจอ**
  เคยเป็นแบบนั้นแล้วบนจอโน้ตบุ๊กมีพื้นดำเปล่าสูงเกือบ 300px คั่นกลางทุกฉาก
- **`s.plans` เป็นรายการ ไม่ใช่ช่องเดียว** รับนัดสองคนในวันเดียวกันได้ แต่ไปได้คนเดียว
  กฎ "ไปได้คนเดียว" อยู่ใน `keepPlan()` ไม่ใช่ที่ชั้น UI ถ้าย้ายไปไว้ที่ UI
  วันหนึ่ง UI เปลี่ยน กฎจะหายไปเงียบๆ พร้อมกับความหมายทั้งหมดของการรับนัดซ้อน
  ผิดนัดเพราะไปหาคนอื่นเจ็บกว่าผิดนัดเฉยๆ เพราะเขารู้ว่าเราเลือกใคร
- **ถ้าคืนหนึ่งมีคนทักได้คนเดียว ระบบนัดซ้อนจะไปไม่ถึงเลย** นัดถูกจองเป็นของ "พรุ่งนี้" เสมอ
  `offerSecondChat()` จึงมีไว้ให้คืนเดียวกันมีคนที่สองทักมาได้ ถ้าสนิทกับหลายคนพอ
  (เทสต์สมดุลจับข้อนี้ได้เอง รายงาน "วันที่รับนัดซ้อนกัน 0" ทั้งที่โค้ดรองรับไว้หมดแล้ว)
- **ตัวละครมีชีวิตตอนเราไม่อยู่** `src/sim/offscreen.ts` เดินแรงกดดันของแต่ละคนทุกวัน
  เร็วขึ้นถ้าเราไม่ไปหา ช้าลงตามความสนิทและความเชื่อใจ พอถึงเกณฑ์ เรื่องเกิดขึ้นเอง
  **กติกา: ผู้เล่นต้องได้รู้เสมอ** ทุกเหตุการณ์ถูกจดลงสมุดบันทึกและตั้งธงให้บทหยิบไปใช้
  ถ้าไม่มีใครรู้ว่ามันเกิด ก็ไม่ต่างจากไม่มีระบบนี้เลย
- **เหตุการณ์ลับหลังต้องเป็นผลของการ *เลือก* ไม่ไป ไม่ใช่สิ่งที่เกิดแน่ๆ ทุกรอบ**
  ครั้งแรกที่จูนไว้ เรื่องเกิดหลังไม่แวะแค่ 4 วัน ซึ่งแปลว่าเกิดกับทุกคนเสมอไม่ว่าเล่นยังไง
  ตอนนี้ราว 8 วัน และคนที่สนิท+ไว้ใจถึงระดับสูงจะไม่มีเรื่องเกิดเลย (มีเทสต์คุมข้อนี้)
- **`trustFlags` ช่องที่สามคือสิ่งที่ตัวละครจำได้** แล้วหยิบมาพูดเองในไลน์ตอนกลางคืน
  ink เรียก `recalls(id)` กับ `memoryOf(id)` ได้ · เขียนค่าผ่าน `recall()` เท่านั้น
- **ความสัมพันธ์มีสองแกน ไม่ใช่แกนเดียว** `s.affinity` คือความสนิท `s.trust` คือความเชื่อใจ
  แกนเดียวทำให้ "ทำให้เขาสบายใจ" กับ "ทำในสิ่งที่ถูก" ให้ผลเหมือนกันหมด ซึ่งไม่จริง
  **ต้องเรียกผ่าน `changeAffinity()` / `changeTrust()` เท่านั้น** ห้ามเขียน `s.trust[id] += n` ตรงๆ
  ความสนิทกระจายไปทั้งวงเสมอ · ความเชื่อใจกระจายเฉพาะตอนเสีย (เรื่องแบบนั้นไปถึงหูกันเสมอ)
- **คำพูดถูกผูกกับ *วัน* ที่พูด ไม่ใช่แค่หัวข้อ** `src/sim/claims.ts`
  "เมื่อวานหายไปไหน" ที่ถามคืนนี้ กับที่ถามอีกสองสัปดาห์ คนละเมื่อวานกัน
  ถ้าเก็บรวมใต้หัวข้อเดียว การตอบคนละอย่างคนละเดือนจะถูกนับเป็นโกหก ซึ่งไม่จริง
  โป๊ะได้ต่อเมื่อบอกสองคน **ในคืนเดียวกัน** ไม่ตรงกัน แล้วเขาสองคนคุยกันวันรุ่งขึ้น
  (จึงต้องมี `offerSecondChat()` ก่อน ระบบนี้ถึงจะไปถึงได้เลย)
  **`bonds` ที่ติดลบทำให้โกหกรอด** ปลอยกับกนินไม่ถูกกัน (−0.3) บอกสองคนนี้ไม่ตรงกันไม่มีวันโป๊ะ
  เป็นเรื่องตั้งใจ — แผนที่ว่าใครคุยกับใครคือกติกาของระบบนี้ทั้งระบบ
  **ความเชื่อใจมีพื้นที่ 0** โกหกคนที่ยังไม่ไว้ใจเราอยู่แล้วจึงไม่เสียอะไรในแกนนั้น
  ราคาไปตกที่ชื่อเสียงแทน ถ้าจะปรับให้เจ็บกว่านี้ ปรับ `claims.standingHit` ก่อน `trustHit`

- **`data/game.json > trustFlags` แปลงธงจากบทเป็นความเชื่อใจให้อัตโนมัติ**
  เขียนทางเลือกใหม่ที่ "ทำสิ่งที่ยาก" แล้วใส่ธงลงตารางนี้ ไม่ต้องเรียก `gainTrust` ในบท
  `npm run balance` มีบรรทัด "สองแกนแยกกันจริงไหม" — ถ้าต่ำกว่า 20% แปลว่าแยกมาแล้วไม่ได้อะไร
- **กระดานประกาศผลหน้าห้องเป็นของสาธารณะ สมุดพกเป็นของเรา** `src/sim/board.ts`
  เพื่อนทุกคนมีคะแนนของตัวเอง มาจากสามอย่าง: ความสามารถพื้นฐาน (`smart` ใน characters.json) ·
  จำนวนครั้งที่เราติวให้ · และ**แรงกดดันในชีวิตที่เราปล่อยไว้** (`lifeOf().pressure`)
  ไม่ไปหาเขาทั้งเดือนไม่ได้จบแค่เขาเหงา มันมาโผล่บนกระดานที่ทั้งห้องยืนอ่าน
  **แรงกดดันต้องหารด้วย `offscreen.threshold` ก่อนเสมอ** มันเป็นเลขดิบ 0-26
  เอาไปคูณตรงๆ คะแนนของทุกคนจะติดศูนย์ทั้งกระดานตั้งแต่สัปดาห์แรก
  **กระดานถูกเก็บแยกตามภาค** ชื่อรอบสอบของมัธยมกับปีหนึ่งซ้ำกัน ("midterm"/"final")
  ถ้าไม่แยก กระดานของปีหนึ่งจะไม่เคยถูกติดเลยเพราะช่องเดิมมีของอยู่แล้ว (เจอตอนเทสต์สมดุล
  รายงานอันดับของเด็กหลังห้องเป็น 5 ทั้งที่คะแนนสอบบอกว่า 16)
- **`s.flags[\`beat_${id}\`]` แบบประกอบชื่อจากตัวแปร `npm run story` มองเห็นแล้ว**
  มันกางชื่อออกตามรายชื่อตัวละครจริงก่อนเทียบ ธงที่ตั้งจากโค้ดจึงต้องมีคนอ่านเหมือนธงจากบท
  แปลว่าธงแบบนี้ต้องตั้งให้ครบทุกคน หรือไม่ก็อย่าตั้งเลย — ตั้งครึ่งๆ จะทำให้เทสต์ล้ม
- **แรงเป็นทางเลือก ไม่ใช่กำแพง** `src/sim/push.ts`
  เดิมต่ำกว่า `energy.lowThreshold` แล้วปุ่มทุกปุ่มดับ = เกมตัดสินใจแทนว่าวันนี้พอแค่นี้
  ตอนนี้ฝืนได้เสมอ แต่ได้ของ 85% · เป็นหนี้การนอน · หนี้มากพอจะหลับในคาบและล้มป่วยทั้งวัน
  **หนี้การนอนต้องทยอยใช้คืน (`energy.debtRecover`) ห้ามล้างทิ้งทุกเช้า**
  ของเดิมล้างทุกเช้า แปลว่าฝืนสิบคืนติดเท่ากับฝืนคืนเดียว และผลที่เกิด*ระหว่างวัน*
  (หลับในคาบ) จะไม่มีวันเห็นหนี้เลยสักครั้ง เพราะตอนนั้นหนี้เป็น 0 ไปแล้ว
  **`stepSickness()` ต้องอยู่หลังฟื้นแรงแต่ก่อนล้างหนี้** ผิดลำดับข้อใดข้อหนึ่งมันจะไม่เกิดเลย
  **ข้อค้นพบจาก `npm run balance`: ฝืนเพื่อเก็บค่าสถานะเฉยๆ ไม่มีวันคุ้ม**
  เพราะค่าสถานะอิ่มตัวอยู่แล้ว (`gainFactor`) และการพักก็แค่เลื่อนไปทำพรุ่งนี้
  มันคุ้มเฉพาะตอนที่ของวันนี้เอาไปทำพรุ่งนี้ไม่ได้ กล่องยืนยันจึงบอกผู้เล่นตรงๆ แบบนั้น
  เทสต์สมดุลจึงไม่ได้การันตีว่า "ฝืนแล้วต้องชนะ" แต่การันตีว่า **ฝืนสุดตัวต้องแพ้** ทั้งสองแบบ
- **`ActionResult.ok` คือคำตอบว่าทำได้ไหม ห้ามเดาจากข้อความ**
  ฝั่ง UI เคยใช้ `/หมดแรง|แรงเหลือน้อย|เงินไม่พอ/.test(message)` ซึ่งพังเงียบทันทีที่มีเหตุผลใหม่
- **เสียงที่นิยามไว้ใน `audio.ts` แต่ไม่มีใครเรียก = เหตุการณ์ที่เกิดแล้วเงียบ**
  `npm run story` นับให้แล้วและล้มถ้ามีอันไหนเงียบ เคยมีเจ็ดอันนิยามไว้เฉยๆ
  (dull day exam stood recall push board) ซึ่งไม่มี error ให้เห็นและเล่นเองก็ไม่รู้ว่าขาดอะไร
- **งานใหญ่ของชมรมเกิดในวันเหตุการณ์ที่ปฏิทินมีอยู่แล้ว** `src/sim/milestone.ts`
  `milestoneDay` ใน clubs.json ตรงกับวันของ field_trip (63) · sports_day (77) · scout_camp (95)
  ผลมาจาก **จำนวนครั้งที่ไปซ้อมทั้งเทอม** เป็นฐาน มินิเกมเป็นตัวคูณ แบบเดียวกับข้อสอบ
  มินิเกมอย่างเดียวชดเชยการไม่ซ้อมไม่ได้ (มีเทสต์คุมข้อนี้) เพราะไม่งั้นชมรมจะไม่มีความหมาย
  **`s.clubDays` ถูกล้างตอนขึ้นปีหนึ่ง** เทสต์ต้องจำค่าไว้ตอนวันงาน ไม่ใช่ไปอ่านตอนจบเทอม
  (เจอตอนรายงานขึ้นว่า "ซ้อมเฉลี่ย 0 ครั้ง" ทั้งที่ทุกกลยุทธ์ไปซ้อมสัปดาห์ละสองวัน)
- **ชื่อธงต้องเขียนตรงๆ ห้ามประกอบจากเงื่อนไข** `s.flags[ok ? "a" : "b"]` กับ
  ``s.flags[`x_${id}_${ok ? "won" : "lost"}`]`` `npm run story` มองไม่เห็นทั้งคู่
  เขียนเป็น `if (ok) s.flags["a"] = true; else s.flags["b"] = true;` แทนเสมอ
- **การเลือกมีพยาน** `src/sim/seen.ts` — `where` ใน characters.json บอกอยู่แล้วว่าใครอยู่ที่ไหน
  ตอนไหน ไปหาพลอยที่ห้องสมุดหลังเลิกเรียน ปาล์มก็นั่งอยู่ตรงนั้นเหมือนกัน
  **คู่ที่อยู่ที่เดียวกันได้มีแค่ ploy↔palm (ห้องสมุด) กับ tar↔nun (โรงอาหารคณะ)**
  ถ้าเพิ่มตัวละครใหม่แล้วอยากให้ระบบนี้ทำงานกับเขา ต้องให้ `where` ชนกับใครสักคน
  ราคาไม่เท่ากันสองแบบ: คนที่ *นัดเราไว้วันนี้* แล้วเห็นกับตา (แรง) กับคนที่แค่สนิทพอจะรู้สึก (เบา)
- **"ระเบิด" แบบ Tokimeki Memorial อยู่ใน `stepLives()`** พอเรื่องลับหลังเกิดกับใครสักคน
  เขาเล่าให้เพื่อนในกราฟ `bonds` ฟัง แล้วทั้งวงเย็นชาลงพร้อมกัน
  ระลอกปกติของ `changeAffinity()` เล็กเกินกว่าจะรู้สึก — อันนี้คือระลอกที่ตั้งใจให้รู้สึก
  และเกิดเฉพาะตอนที่เราปล่อยเขาไว้จนมีเรื่องจริงๆ เท่านั้น
- **ทางบ้านคือที่ที่เงินของภาคมัธยมไหลออก** `src/sim/home.ts` · `story/ev_home_ask.ink`
  ภาคมหาลัยมีค่าหออยู่แล้ว แต่มัธยมไม่เคยมีอะไรดูดเงินออกเลย (เด็กหลังห้องจบเทอมด้วย 32,000 บาท)
  **จำนวนที่ขอต้องอิงกับค่าขนมทั้งเทอม (ราว 4,000 บาท)** ตอนแรกตั้ง askBase 900 + 2200×stage
  ผลคือวันที่ 20 ขอ 1,267 ซึ่งคนที่ไม่ได้ทำงานพิเศษไม่มีทางมี — ทุกกลยุทธ์เลยตอบว่า
  "ไม่มีจริงๆ" ซึ่งไม่นับว่าปฏิเสธ แปลว่าการเลือกทั้งระบบหายไปเงียบๆ
  ไม่ให้ทั้งที่มี ≠ ไม่มีจริงๆ — อย่างหลังไม่ใช่การเลือก จึงไม่นับเป็นการปฏิเสธ
  บ้านที่ตึงไม่ได้ลงโทษด้วยตัวเลขตรงๆ มันกดค่าขนมลง (เขาไม่มีจะให้) และทำให้นอนไม่หลับ
  ซึ่งไปบวกกับหนี้การนอนของระบบฝืน
- **ครูประจำชั้นเป็นระบบ ไม่ใช่ตัวละครที่ห้า** `src/sim/teacher.ts` · `s.teacher` 0-100
  ไม่มี portrait ไม่มี `where` ไม่มีไฟล์บทของตัวเอง — เขาพูดผ่าน `assembly.ink` และผ่านผลที่เกิดขึ้น
  **ค่านี้ขยับจากความรับผิดชอบ ไม่ใช่จากการไปหา** ส่งการบ้าน ตัดผม ไปสอบซ่อม = บวก ·
  ไม่ส่งงาน โดนจับ โดนเรียกหน้าแถว = ลบ · ต้องผ่าน `noteBehaviour()` เท่านั้น
  **มันไปบรรจบกับสองระบบที่มีอยู่แล้ว** ครูที่ไว้ใจเราลดโทษตอนโดนฝ่ายปกครองจับ ·
  ครูที่ไม่ไว้ใจเราโทรหาที่บ้าน ซึ่งไปเพิ่ม `home.strain` โดยตรง
  **การโทรต้องมีคูลดาวน์และความตึงต้องมีเพดาน** ตอนแรกโทรได้ทุกวันจันทร์ ผลคือ
  เด็กหลังห้องจบเทอมด้วยบ้านตึง 22 ทั้งที่ระดับสูงสุดอยู่ที่ 4 — ตัวเลขวิ่งเลยสเกลไปไกล
  จนระดับไม่มีความหมาย และไม่มีทางกลับตัวได้เลย
- **คู่แข่งอยู่ใน `src/sim/rival.ts` และไม่มีตัวตนบนจอ** เป็นชื่อที่ตัวละครเอ่ยถึงเอง
  (`rival` ใน characters.json) เหมือนครูประจำชั้น — ไม่มีภาพ ไม่มีบท ไม่เคยปรากฏตัว
  **คะแนนคู่แข่งต้องอยู่สเกลเดียวกับ `affinityRanks` (0-65) ไม่ใช่ 0-100**
  ตอนแรกตั้ง 0-100 แล้วคู่แข่งแซงทุกคนทุกรอบ เพราะความสนิทที่ทำได้จริงในหนึ่งเทอม
  อยู่แถว 23 เท่านั้น (เพดานคู่แข่ง 32 จึงแปลว่าสนิทเกิน ~37 แล้วเขาไล่ไม่ทันอีกเลย)
  **เกณฑ์ `minRank` ใช้ครั้งเดียวตอนเริ่ม ห้ามเช็กซ้ำทุกวัน** เพราะเรื่องลับหลัง
  กับข่าวที่แพร่ไปทั้งวงกดความสนิทลงได้เอง ถ้าเช็กซ้ำ ความสนิทที่ตกจะไป *หยุด* คู่แข่งไว้
  ซึ่งกลับหัวกลับหาง — ยิ่งเราทิ้งเขา คู่แข่งยิ่งควรได้ที่ ไม่ใช่ยิ่งหยุดเดิน
  **ทางออกไม่ใช่การกำจัดคู่แข่ง** มีสองทางเท่านั้น: ลงเวลาให้มากกว่า หรือยินดีกับเขา
  แล้วได้ความเชื่อใจแทน (`concede()` — ความสนิทหยุดโต แต่คนที่พูดแบบนั้นได้มีไม่เยอะ)
- **ของฝากเจาะจงเป็นรายคน ไม่ใช่รายประเภท** `wants` / `dislikes` ใน characters.json
  เดิมจับคู่ด้วย `likes` ซึ่งเป็นค่าสถานะ แปลว่าคนที่ค่าสถานะคล้ายกันชอบของเหมือนกันหมด
  ของที่เขาอยากได้จริงๆ ได้ทั้งความสนิทและความเชื่อใจ · ของที่ผิดฝาผิดตัว **เสีย** ความเชื่อใจ
  เพราะมันบอกเขาว่าเราไม่ได้ดูเลยว่าเขาเป็นคนยังไง · ของเดิมซ้ำๆ ได้ใจน้อยลงทุกครั้ง
  **ปุ่มในกระเป๋าบอกใบ้ให้เฉพาะคนที่สนิทถึง `gift.knowAtRank` แล้ว** (♥ = เขาอยากได้ · ↺ = เคยให้แล้ว)
  ถ้ารู้ตั้งแต่แรก การรู้จักเขาก็ไม่มีความหมาย
- **ฉากหลังอยู่ใน `src/ui/backdrop.ts`** เพิ่มสถานที่ใหม่ต้องเพิ่มฉากด้วย ไม่งั้นจะได้ฉากบ้านแทน
- **`translateY` เป็นเปอร์เซ็นต์อิงขนาดของตัวเอง ไม่ใช่ของกล่องแม่** เคยทำให้โน้ตในมินิเกมดนตรี
  ตกได้แค่ 24px ต้องคำนวณระยะเป็นพิกเซลจาก `clientHeight` ของเวที
- **มินิเกมคืนคะแนน 0..1 เสมอ** ฝั่งเกมหลักเอาไปคูณกับผลลัพธ์ (คะแนนสอบ ค่าสถานะจากชมรม)
  ไม่ใช่ตัวตัดสินทั้งหมด ความรู้ที่สะสมมาทั้งเทอมยังเป็นฐานหลัก

## แผนที่ไฟล์

```
src/sim/      ตรรกะเกมล้วน — state, calendar, economy, actions, exam, club, shop, discipline,
              chat, homework, schoolwork (สอบซ่อม + งานกลุ่ม), grooming (ตรวจหน้าเสาธง),
              grades (เกรดรายวิชา),
              bonds (ระลอกข้ามตัวละคร + ชื่อเสียง + การเลือกข้าง), chapter (ภาค), ending
src/story/    bridge.ts — สะพาน ink ↔ TS (ฉีดตัวแปรเข้า, external function กลับออก)
src/ui/       scene.ts (กล่องบทสนทนา + ไล่ตัวอักษร) · panels.ts (คนรู้จัก กระเป๋า ปฏิทิน เมนู ผลสอบ สรุปเทอม)
              portrait.ts (ภาพตัวละคร SVG) · backdrop.ts (ฉากหลังสถานที่/เหตุการณ์ SVG)
              icons.ts (ไอคอน) · theme.ts (บรรยากาศตามช่วงเวลา) · minigame.ts (มินิเกมทั้งสี่)
              chat.ts (หน้าจอไลน์ ฟองซ้ายคือเขา ฟองขวาคือเรา)
src/core/     save.ts (เซฟ 4 ช่องลง localStorage) · rng.ts (mulberry32 สำหรับเทสต์ที่ต้องซ้ำได้)
              audio.ts (เสียงสังเคราะห์สด ไม่มีไฟล์เสียง — ไฟล์เดียวกับฝั่ง genesis)
src/test/     inkcheck.ts (lint:ink) · storywalk.ts (story) · balance.ts (balance)
story/        บททั้งหมด 27 ไฟล์ — ตัวละคร 6 คน (มัธยม 4 · มหาลัย 2) · แชทคนละไฟล์ ·
              เหตุการณ์ของทั้งสองภาค · assembly.ink (เข้าแถว)
```
